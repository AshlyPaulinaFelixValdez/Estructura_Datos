// Declaración
int[] numeros = new int[4];

// Asignación
numeros[0] = 10;
numeros[1] = 20;

// Leer
Console.WriteLine(numeros[1]); // imprime 20 

// Analizar
foreach(int n in numeros) {
    console.WriteLine(n);
}